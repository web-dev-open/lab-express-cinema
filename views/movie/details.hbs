<div>
    hey
    <img src="{{movie.image}}" alt="">
    <div>
        <a href="/movies"></a>
        <h1>{{movie.title}}</h1>
        <h2>{{movie.director}}</h2>
        <p>Stars</p>
        <ul>Stars
            <li>{{#each movie.stars}}</li>
        </ul>
        <p>{{movie.description}}</p>
        <ul>
            <li>{{#each movie.showtimes}}</li>
        </ul>
    </div>
    
</div>
<div></div>